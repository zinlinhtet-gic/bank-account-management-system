<?xml version='1.0' encoding='utf-8'?>
<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:t="clr-namespace:bams.desktop.Themes" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:Class="bams.desktop.Views.Pages.AccountListView" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">
  <UserControl.Resources>
    <Style TargetType="TextBlock" BasedOn="{StaticResource Text.Base}" />
  </UserControl.Resources>
  <Grid Margin="28">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <DockPanel Margin="0,0,0,20">
      <Button DockPanel.Dock="Right" Content="＋  Create Account" Style="{StaticResource Button.Primary}" Command="{Binding Workflow.OpenCreateCommand}" MinWidth="170" MaxWidth="230" Height="44" />
      <StackPanel>
        <TextBlock Text="Accounts" Style="{StaticResource Text.Heading}" FontSize="28" />
        <TextBlock Text="Search, filter, and manage account records" Style="{StaticResource Text.BodyMuted}" Margin="0,4,0,0" />
      </StackPanel>
    </DockPanel>
    <Border Grid.Row="1" Background="{StaticResource CardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,14">
      <WrapPanel VerticalAlignment="Center">
        <TextBox Width="260" Margin="0,0,10,0" Style="{StaticResource Input.Text}" Text="{Binding Workflow.SearchText, UpdateSourceTrigger=PropertyChanged}" t:ThemeAssist.Placeholder="Account number" />
        <ComboBox Width="220" Margin="0,0,10,0" Style="{StaticResource Input.Select}" ItemsSource="{Binding Workflow.AccountTypes}" SelectedItem="{Binding Workflow.SelectedFilterType}" DisplayMemberPath="Name" t:ThemeAssist.Placeholder="All account types" />
        <ComboBox Width="165" Margin="0,0,10,0" Style="{StaticResource Input.Select}" ItemsSource="{Binding Workflow.StatusOptions}" SelectedItem="{Binding Workflow.SelectedStatus}" t:ThemeAssist.Placeholder="All statuses" />
        <Button Content="Apply filters" Style="{StaticResource Button.Secondary}" Command="{Binding Workflow.SearchAccountsCommand}" />
      </WrapPanel>
    </Border>
    <Grid Grid.Row="2">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <TextBlock Text="{Binding Workflow.ErrorMessage}" Style="{StaticResource Text.Error}" Margin="0,0,0,8" />
      <DataGrid Grid.Row="1" ItemsSource="{Binding Workflow.Accounts}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" RowHeight="48" Background="{StaticResource CardBrush}" BorderBrush="{StaticResource BorderBrush}" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" MinWidth="760">
        <DataGrid.RowStyle>
          <Style TargetType="DataGridRow">
            <EventSetter Event="MouseLeftButtonUp" Handler="AccountRow_MouseLeftButtonUp" HandledEventsToo="True" />
          </Style>
        </DataGrid.RowStyle>
        <DataGrid.Columns>
          <DataGridTextColumn Header="ACCOUNT NUMBER" Binding="{Binding AccountNo}" ElementStyle="{StaticResource Text.Mono}" Width="2*" MinWidth="190" />
          <DataGridTextColumn Header="TYPE" Binding="{Binding AccountTypeCode}" Width="*" MinWidth="120" />
          <DataGridTextColumn Header="STATUS" Binding="{Binding Status}" Width="*" MinWidth="120" />
          <DataGridTemplateColumn Header="ACTIONS" Width="220" MinWidth="220">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <StackPanel Orientation="Horizontal">
                  <Button Content="Change Status" Style="{StaticResource Button.Ghost}" Command="{Binding DataContext.Workflow.OpenStatusDialogCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" />
                  <Button Content="Suspend" Foreground="{StaticResource DangerBrush}" Style="{StaticResource Button.Ghost}" Command="{Binding DataContext.Workflow.SuspendAccountCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" />
                </StackPanel>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
        </DataGrid.Columns>
      </DataGrid>
      <DockPanel Grid.Row="2" Margin="0,12,0,0">
        <TextBlock Text="{Binding Workflow.InfoMessage}" Style="{StaticResource Text.Caption}" VerticalAlignment="Center" />
        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
          <Button Content="Previous" Style="{StaticResource Button.Secondary}" Command="{Binding Workflow.PreviousPageCommand}" Margin="0,0,8,0" />
          <TextBlock Text="{Binding Workflow.PageLabel}" Style="{StaticResource Text.Caption}" VerticalAlignment="Center" Margin="0,0,8,0" />
          <Button Content="Next" Style="{StaticResource Button.Secondary}" Command="{Binding Workflow.NextPageCommand}" />
        </StackPanel>
      </DockPanel>
      <TextBlock Grid.Row="1" Text="Loading accounts…" Style="{StaticResource Text.BodyMuted}" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding Workflow.IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
    </Grid>
  </Grid>
</UserControl>
