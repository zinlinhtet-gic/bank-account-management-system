<UserControl x:Class="bams.desktop.Views.Pages.Configuration.OtherBanksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1000">

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="32,28,32,20">
            <TextBlock Style="{StaticResource PageTitleTextStyle}" Text="{Binding PageTitle}"/>
            <TextBlock Margin="0,4,0,0" Style="{StaticResource PageSubtitleTextStyle}" Text="{Binding PageDescription}"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="32,0,32,32" Style="{StaticResource CardBorderStyle}">
            <StackPanel>
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center" Style="{StaticResource SectionTitleTextStyle}" Text="Correspondent Banks"/>
                    <Button Grid.Column="1" Content="Refresh" Style="{StaticResource SecondaryButtonStyle}"/>
                </Grid>

                <DataGrid ItemsSource="{Binding Rows}" Style="{StaticResource ConfigDataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="0.8*" Binding="{Binding BankCode}" Header="Bank Code"/>
                        <DataGridTextColumn Width="1.6*" Binding="{Binding BankName}" Header="Bank Name"/>
                        <DataGridTextColumn Width="1*" Binding="{Binding SwiftCode}" Header="SWIFT Code"/>
                        <DataGridTextColumn Width="0.8*" Binding="{Binding Status}" Header="Status"/>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock
                    Margin="0,20,0,0"
                    HorizontalAlignment="Center"
                    Style="{StaticResource SectionNoteTextStyle}"
                    Text="No correspondent banks yet."
                    Visibility="{Binding Rows.Count, Converter={StaticResource CountToVisibility}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
